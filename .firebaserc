{
  "_comment": "Replace 'your-firebase-project-id' with your Firebase project ID, or run `firebase use --add` to configure interactively.",
  "projects": {
    "default": "your-firebase-project-id"
  }
}
